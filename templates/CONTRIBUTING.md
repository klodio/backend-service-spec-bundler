# {Project Name} — Developer Rules

These rules are non-negotiable. Every contributor must follow them.

<!--
Generate 5-10 rules derived from the project's architectural decisions and constraints.
Each rule should follow this pattern:

## {N}. {Short Rule Name}

{Why this rule exists — 1-2 sentences connecting it to an architectural decision or constraint.}

**Do this:**
```{language}
// Good example
```

**Not this:**
```{language}
// Bad example — explain why
```

Common categories of rules:
- Deployment model (how the system is packaged/run)
- Code style preferences (flat vs. deep, utils vs. classes, etc.)
- Performance patterns (allocation, caching, concurrency)
- Spec maintenance (keep docs in sync with code)
- Configuration format
- Decision recording (when to write ADRs)
- Testing requirements (no bugfix without a test)
- Branching/PR conventions
- Domain-specific rules (e.g., protocol addition criteria, API versioning policy)
-->
